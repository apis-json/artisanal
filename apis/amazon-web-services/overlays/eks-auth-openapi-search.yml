openapi: 3.1.0
info:
  title: eks-auth
paths:
  /clusters/{clusterName}/assume-role-for-pod-identity:
    POST:
      summary: AssumeRoleForPodIdentity
      description: >-
        <p>The Amazon EKS Auth API and the <code>AssumeRoleForPodIdentity</code>
        action are only used by the EKS Pod Identity Agent.</p> <p>We recommend
        that applications use the Amazon Web Services SDKs to connect to Amazon
        Web Services services; if credentials from an EKS Pod Identity
        association are available in the pod, the latest versions of the SDKs
        use them automati
      tags:
        - Assume
        - Roles
        - For
        - Pods
        - Identity
        - Names
        - Assume
        - Roles
        - For
        - Pods
        - Identi
